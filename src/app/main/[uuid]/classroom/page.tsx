import Classroom from "@/components/Classroom";

export default function ClassroomPage() {
    return (
        <div className="flex justify-center w-[100vw] max-w-full h-[100vh]">
            <Classroom />
        </div>
    );
}
